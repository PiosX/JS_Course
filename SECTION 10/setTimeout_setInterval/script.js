const test = () => {
	console.log(":D");
};

// test();

// setTimeout - wykonuje się po podanym czasie
setTimeout(test, 1000);

//setInterval - wykonuje sie co okreslona ilosc czasu
setInterval(test, 1000);
